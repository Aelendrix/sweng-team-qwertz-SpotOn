package ch.epfl.sweng.spotOn.test;

// I commented this tests because it's been done a while ago, and no longer represents a good test for the app in the state it is nows

/**
 * Created by olivi on 10.10.2016.
 */
/*
        import android.app.Activity;
        import android.support.test.InstrumentationRegistry;
        import android.test.ActivityInstrumentationTestCase2;

        import ch.epfl.sweng.spotOn.gui.TakePictureFragment;

        import static android.support.test.espresso.Espresso.onView;
        import static android.support.test.espresso.action.ViewActions.click;
        import static android.support.test.espresso.action.ViewActions.typeText;
        import static android.support.test.espresso.assertion.ViewAssertions.matches;
        import static android.support.test.espresso.matcher.ViewMatchers.withId;
        import static android.support.test.espresso.matcher.ViewMatchers.withText;

public class PictureCaptureTest extends ActivityInstrumentationTestCase2<TakePictureFragment> {
    public PictureCaptureTest() {
        super(TakePictureFragment.class);
    }

    @Override
    public void setUp() throws Exception {
        super.setUp();
        injectInstrumentation(InstrumentationRegistry.getInstrumentation());
    }

    public void testCanDisplayImage(){
        //onView(withID(R.id.captureButton)).perform(dispatchTakePictureIntent(this.findViewById(R.id.activity_picture)));
    }
}
*/